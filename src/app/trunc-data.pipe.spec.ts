import { TruncDataPipe } from './trunc-data.pipe';

describe('TruncDataPipe', () => {
  it('create an instance', () => {
    const pipe = new TruncDataPipe();
    expect(pipe).toBeTruthy();
  });
});
